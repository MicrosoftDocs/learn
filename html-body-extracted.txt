

Training Outline: Building AI Agents with GitHub Copilot SDK in Enterprise
Applications

This training module is organized into a sequence of units
that progressively build understanding and skills for integrating AI agents
into enterprise applications using the GitHub Copilot SDK. It starts
with conceptual foundations (what agents are and how the Copilot SDK enables
them), then examines real-world inspired scenarios, and finally dives into
implementation patterns�covering both common requirements and scenario-specific
considerations�before a hands-on lab exercise. The outline ensures that learners
first grasp the what and why, then the how,
and ultimately apply their knowledge in practice. Units are grouped logically:
Units 1�3 cover fundamentals and use cases, Units 4�6 cover implementation
techniques (from basic to advanced), and Unit 7 is a capstone lab.

Unit 1: Introduction to AI Agents (Concepts & Capabilities)

�       
What is an AI Agent? � Define AI agents
and how they differ from basic chatbots. Emphasize that an AI agent is �capable
of reasoning, taking action, and adapting in real time�, not just
responding to queries[1].
It can orchestrate multi-step workflows
across systems, whereas a simple bot cannot.

�       
Why Agents in Enterprise? � Explain the
motivations for agentic AI in business: handling complex or repetitive tasks
autonomously, improving efficiency and consistency, and leveraging AI to
augment human work. For example, unlike a FAQ bot, an agent could execute
tasks (reset a password, approve a request) by interacting with enterprise
systems[2]
[3].

�       
Key Capabilities � Outline core
capabilities of modern AI agents:

o  
Natural Language Understanding (to truly
grasp user intent and context, beyond keywords).

o  
Reasoning & Planning (to break down
goals into steps and make decisions).

o  
Tool Usage / Action Execution (to call
APIs, databases, or other services to carry out tasks).

o  
Learning/Adaptation (optional mention:
improving from feedback over time, though in our context we�ll focus on initial
development).

�       
When to Use AI Agents � Discuss scenarios
where an AI agent is appropriate: when tasks require automation of workflows,
integration of multiple systems, or intelligent decision-making. Also note when
not to use an agent (simple Q&A might not need a full agent, etc.), to set
scope.

�       
Examples Preview � Briefly hint at
upcoming scenario examples (e.g., �In this course, we�ll see agents that handle
customer orders, sales leads, finance reconciliations, and more�), to spark
interest and show real-world relevance.

Unit 2: Overview of the GitHub Copilot SDK (Platform for AI Agents)

�       
What is the Copilot
SDK? � Introduce the GitHub Copilot SDK as a multi-platform toolkit for
embedding AI Copilot agents into apps[4].
It provides a �production-tested execution loop� and a high-level API so
you don�t have to build an agent from scratch[5].

�       
Key Features � Highlight the major
features of Copilot SDK:

o  
Agentic Execution Loop � A built-in
planner that manages multi-turn dialogues and tool invocation (the same core
that powers Copilot CLI).

o  
Tool Orchestration � Support for defining
custom tools/commands that the agent can invoke autonomously[6].

o  
Multi-Model Support � Ability to use
multiple AI models or switch models for different tasks (e.g., GPT-4, code
specific models, etc.)[7].

o  
Persistent Context & Memory � Manages
context across turns with intelligent compaction, enabling long-running
sessions[8].

o  
Real-Time Streaming � Can stream agent
responses (useful for interactive UI feedback)[9].

o  
Multi-language SDK � Available in
Node.js, Python, Go, and .NET (so you can use C# for our lab)[10].

o  
Integration & Security � Integration
with GitHub authentication and (optionally) the MCP for enterprise data
connectivity[11].

�       
How the SDK Simplifies Agent Building �
Explain the problems it solves for developers:

o  
Handles context management, tool routing, model
handling, permissions, and failure modes out-of-the-box[12].

o  
Essentially, �instead of wiring your own
planner and tool loop, you embed Copilot�s agentic loop�, saving
significant development effort[13].

�       
Architecture at a Glance
� Provide a high-level view of how an application interacts with Copilot SDK:

o  
The app initializes the SDK client and starts an
agent session (choose model, etc.).

o  
The developer defines skills/tools
(functions or API calls) that the agent can use.

o  
At runtime, the agent (backed by an LLM)
receives a user query or task request, plans a solution, calls the necessary
tools via the SDK, and returns a result.

o  
(We won�t dive into code here,
but set the mental model for how it plugs in).

�       
Constraints & Considerations � Note
that while the SDK is powerful, the developer must still provide domain
knowledge (in the form of tools and guidance). Also mention it�s in Technical
Preview (as of early 2026) so some features or performance aspects are evolving[14].

Unit 3: Enterprise AI Agent Use Cases � Five Scenarios Beyond Chatbots

�       
Purpose of this Unit: Illustrate meaningful,
real-world scenarios where AI agents provide business value, to ground the
abstract concepts. Learners see what�s possible and impactful, strengthening
the �why� before we teach �how.�

�       
Scenario Overviews: Present five distinct
enterprise scenarios (introduced in the previous Q&A) that go beyond a
simple Q&A bot. For each scenario, include:

o  
Name & Domain � e.g., E-commerce
Customer Service Agent (Retail), Sales Lead Management Agent (CRM), Finance
Automation Agent, Supply Chain Planning Agent, IT Ops Incident
Agent.

o  
Use Case Description � 2-3 sentences on
what the agent does in that scenario:

i.     
E-commerce Customer Service & Operations
Agent � Handles customer requests (returns, order tracking) by interacting
with order databases and fulfillment systems to resolve issues automatically,
not just answer questions.

ii.     
Sales CRM Lead
Management Agent � Automates sales ops tasks (qualifying leads, data entry,
drafting follow-ups) by pulling info from external sources and updating CRM
records.

iii.     
Finance & Accounting Automation Agent
� Reconciles transactions and processes invoices by cross-checking data across
systems, flagging anomalies for humans (reducing tedious accounting work).

iv.     
Supply Chain & Inventory Management Agent
� Monitors stock levels and external factors, predicts demand, and recommends
actions (like reorders or transfers) to prevent shortages or overstock.

v.     
IT Operations & Incident Response Agent
� Watches system alerts, diagnoses issues (by analyzing logs/metrics), and
takes initial remediation steps (restarting services, etc.) to fix problems or
escalate intelligently.

o  
Value Proposition � Highlight the
business benefit of each (e.g., faster customer service resolution, increased
sales productivity, fewer finance errors, proactive inventory control, reduced
IT downtime).

�       
Common Thread � Emphasize what these
scenarios have in common: the AI agents are acting on the user�s behalf
within various enterprise systems (commerce, CRM, ERP, IT monitors) to achieve
specific goals. They maintain context and perform multi-step operations,
demonstrating the versatility of agents across domains[15].
Unlike chatbots that might only retrieve info, these agents execute tasks
and return outcomes.

�       
Fictitious but Realistic � Note that
while we describe these through fictitious companies (�ContosoShop�
for retail, etc.), they are based on real patterns seen in industry. (For
instance, many finance departments are piloting AI for reconciliation, and many
retailers use AI assistants for basic support). This shows the content is
relevant to what developers may encounter. (No real company names will be
used in the training content, to keep it generic.)

�       
Segu� to
Implementation: Prepare learners for the shift to technical how-to. E.g.,
�Next, we will look at how to actually build such
agents. First, we�ll discuss the common technical ingredients these scenarios
share, then tackle what�s unique for each situation.�

Unit 4: Designing AI Agents � Core Components and Common Architecture

�       
Purpose: Teach the foundational
architecture and components required to implement any AI agent. This unit is
about design and planning an agent solution before coding.

�       
Agent Anatomy � Break down the components
that all AI agents (in these scenarios) typically consist of:

a.     
LLM (Language AI Brain) � The central AI
model that interprets instructions and generates plans/output. It�s what
provides reasoning capabilities (e.g., GPT-4 under the hood via Copilot SDK).

b.    
Tools/Skills Library � The set of actions
the agent can perform: functions that interface with
external systems (databases, APIs, etc.). They�re essentially the agent�s hands
and eyes into the real world[16].

c.     
Planner/Controller � The logic that
decides when to call a tool, what to do next, and how to break a
task into sub-tasks. In Copilot SDK, this is largely handled by the built-in
agent loop (so you don�t write this from scratch)[17].

d.    
Memory/Context Store � Mechanisms to
remember what happened earlier in the conversation or process. This could be
the conversation history or state data that persists across turns (handled via
the SDK�s session context, plus any app-managed state).

e.     
Policies/Guardrails � Constraints on
agent behavior: rules about what it can/can�t do autonomously, thresholds for
decisions, fallback strategies if uncertain (important for enterprise
governance).

�       
Diagram or Mental Model: Present a
conceptual diagram or description of how these pieces interact. For example: User
request -> LLM -> decides to use Tool A -> gets result -> LLM
continues reasoning -> maybe uses Tool B -> returns final answer to user.
All the while, context is maintained and policies are enforced. (This could be
a simple flowchart in concept.)

�       
Common Workflow � Describe the typical
lifecycle of an agent interaction, applicable to most scenarios:

a.     
Initialize � Agent is given context
(system instructions, user query or trigger event).

b.    
Interpret & Plan � LLM decides what
the user needs or what problem to solve, possibly formulating a multi-step plan
(implicitly or explicitly).

c.     
Invoke Tools � Agent calls one or more
tools/actions to gather info or make changes (e.g., query a database, call an
API)[18].

d.    
Iterate if needed � It might refine its
approach based on tool outputs, possibly looping (multi-turn) if the task
requires multiple steps.

e.     
Produce Outcome � Agent returns a result
to the user or triggers the next process (e.g., confirms a refund was
processed, or provides an answer).

f.      
Learn/Adjust � (Optional, more advanced)
The system might log this interaction and any failures for improvement.

�       
Addressing the �How� at a Design Level �
For each component above, provide guidance:

a.     
How to identify what tools you need (analyze the
scenario: e.g., need a �lookupOrder� tool for
e-commerce, �sendEmailDraft� tool for CRM).

b.    
How to formulate good prompts or system messages
for the LLM (to steer it properly for your domain).

c.     
How to handle context: e.g., deciding what
information from the app to feed into the agent�s context (user profile?
relevant knowledge base docs? etc.).

d.    
How to plan for errors: e.g., �What if the tool
call fails? What if the LLM output is not confident?� � define fallbacks or
limits.

�       
Example Walk-through: Take one scenario
(say, the E-commerce agent) and conceptually design its agent:

a.     
List the likely tools (check order status,
create return order, issue refund, send confirmation email).

b.    
Outline how a typical interaction flows through
the above lifecycle with those tools.

c.     
This cements the abstract ideas with a concrete
example, paving the way for actual implementation in later units.

�       
Link to Copilot SDK Features: Point out
that the Copilot SDK provides built-in support for
many of these components:

a.     
The execution loop (planner) is provided[19].

b.    
Tools are integrated via its API (you will
register tool handlers in code).

c.     
Memory is handled by session context, but you
can augment it.

d.    
Policies you implement in code around the SDK
(e.g., wrapping certain actions with approval logic).

e.     
This sets up for the
next unit, which dives into implementation aspects of these.

Unit 5: Implementing Common Patterns with Copilot SDK (Foundation Level)

�       
Purpose: Cover the practical
implementation of the common elements identified in Unit 4. This is where
we bridge design into actual coding strategies (still generic, not specific to
one scenario yet). We assume learners have basic familiarity with the SDK now,
so we focus on how to use it for these patterns.

�       
Setting Up an Agent Session � Outline the
basic code flow to start an agent using Copilot SDK in .NET/C# (since our lab
will use C#):

o  
Initializing the CopilotClient
and authenticating (note: require Copilot subscription or key).

o  
Creating a session with a chosen model (and
possibly specifying options like enabling streaming).

o  
Feeding initial context (system message with
instructions for the agent�s role, etc.).

�       
Defining and Registering Tools (Skills) �
Explain how to implement the agent�s tools:

o  
In Copilot SDK, you can define functions that
the agent can call. Show the structure: each tool has a name, description (for
the AI�s understanding), and a function to execute.

o  
Common types of tools across scenarios: database
query functions, HTTP API calls, calculation utilities, file I/O, etc.

o  
Example: Show pseudo-code or a simplified
snippet for a tool, like CheckOrderStatus(orderId) that queries a database. Emphasize how you would register this with the SDK so
the agent knows it exists.

o  
Mention best practices: keep tools deterministic
and side-effect-contained; ensure they validate inputs; maybe limit their
access/scope for safety.

�       
Orchestrating Tools & Multi-step
Execution � How the agent uses tools in practice:

o  
The Copilot SDK�s
planner will decide when to use which tool. Explain that the developer�s role
is to ensure the agent is aware of the tools (through prompt or tool
registration) and to handle the tool outputs appropriately.

o  
Perhaps discuss Tool Use Example: In a
conversation, the agent might output a special command (the SDK intercepts this
and calls the corresponding function). The result is fed back to the agent,
which continues processing. Walk through this cycle briefly[20].
(No need to get into the syntax of how Copilot SDK does it, just conceptually
how it flows.)

o  
Emphasize reliability: the agent might loop
through several tools � ensure to have a limit or check to avoid infinite
loops, etc., as part of robust design.

�       
Maintaining Context and State �
Implementation of memory:

o  
How to provide context each turn (the Copilot
SDK likely handles conversation history automatically, but
mention any specifics like session objects or how to add additional context
like relevant documents fetched via RAG if needed).

o  
If an agent needs long-term memory (across
sessions), mention that the app may need to store state externally (e.g.,
caching results, or storing conversation in a database) � likely out of SDK�s
immediate scope but an important consideration for enterprise apps.

o  
Example: In the CRM agent, if it has already
pulled company info, you might cache it so subsequent queries don�t call
external API again unnecessarily.

�       
Handling Uncertainty and Errors �
Building safeguards:

o  
Error Handling: When a tool call fails or
returns nothing useful, plan how the agent should respond. Perhaps define an
error-handling tool or have the agent send a friendly failure message.

o  
Timeouts & Fallbacks: For tools that
might be slow (say external API), implement timeouts and maybe alternate
strategies. (This could be as simple as the tool function itself enforcing a
timeout.)

o  
Safety Limits: For each scenario, we have
some limits (like finance agent not moving large sums without approval). Show
how to implement a simple rule check in a tool or before/after agent execution.
E.g., in the e-commerce refund tool, before actually issuing
a refund, if amount > $1000, the function could throw an exception or ask
for approval, which the agent could be instructed to handle by apologizing and
escalating.

o  
Mention the concept of permissions: The
Copilot SDK lets you control what the agent is allowed to do by controlling
which tools it has. Reinforce that giving an agent only the tools it needs
naturally sandbox its abilities (principle of least privilege).

�       
Logging and Monitoring � Common
enterprise requirement to track agent actions:

o  
Suggest implementing logging for each agent
decision or tool invocation. E.g., log every time the agent calls a tool (what
tool and parameters), and log the final outcome. This
is crucial for debugging and auditing (especially in finance or IT ops
scenarios).

o  
If the SDK provides hooks or events for tool
usage or step completion, highlight how to use those for logging. Otherwise,
mention wrapping tool functions to log calls.

o  
Also consider monitoring performance: track how
often the agent succeeds vs fails to complete tasks, to identify where it needs
improvement.

�       
Reusing the Base Across Scenarios:
Emphasize that these implementation patterns (tools, context, error handling,
logging) form a common base applicable to all five scenarios. For
instance:

o  
All agents will use tools (though
different ones) � e.g., both e-commerce and finance agents call databases, just
different data.

o  
All need context management � whether
it�s conversation history with a user (customer support) or a chain of system
actions (IT ops).

o  
All require safety checks � be it
transaction limits, or permission to shut down a server, etc., the mechanism
(if-then logic around agent actions) is similar.

o  
By mastering these in the SDK, developers can
tackle a wide range of agent use cases with minimal changes to the framework.

�       
Transition: Now that common requirements
and patterns are understood, we proceed to discuss the unique needs of each
scenario/domain and how to handle those on top of the base implementation.

Unit 6: Addressing Scenario-Specific Requirements (Advanced Topics)

�       
Purpose: Cover the unique
implementation challenges or advanced requirements that certain scenarios
introduce, which go beyond the common base from Unit 5. Group the discussion
into logical sets so it�s not just five disjoint lists. This unit builds on the
foundation by showing how to extend or modify the base architecture for
different contexts.

�       
Group 1 � Customer-Facing Agents (E-commerce
& Sales CRM):

o  
These agents involve natural language
interactions with end-users (customers or salespeople) and often content
generation.

o  
Conversational Flow & UX:
Implementation considerations for a good user experience:

� 
Designing prompts that
ensure the agent asks clarifying questions if needed (not just one-shot answers).

� 
Maintaining a conversational tone appropriate to
the context (friendly helpful tone for customers, professional tone for sales
communications).

� 
In a UI (chat widget or CRM sidebar), how to
handle multi-turn dialogue: the front-end might need to show typing indicators
as the agent �thinks� (using SDK streaming) and partial responses.

� 
Example: In the e-commerce web chat, if the
issue is not clear, the agent should politely ask for the order number rather
than guessing � that logic can be part of prompt scripting and tool usage.

o  
Personalization & Data Privacy: These
agents may access personal or sensitive data (customer orders, CRM leads).
Ensure:

� 
Proper authentication is in place (the agent
should only retrieve data for the authenticated user, etc.).

� 
Data used in prompts doesn�t leak sensitive info
in responses (e.g., agent should not reveal another customer�s data).

� 
Implementation detail: likely the web API that
front-end calls includes user context, and tools like lookupOrder(user,
orderId) enforce user check.

o  
External Data Integration: Especially for
the CRM agent, pulling data from external sources (web APIs for company info,
social media):

� 
Handling API credentials and rate limits (the
tool functions must manage this).

� 
Data parsing and summarization: the agent might
get raw data (JSON) from a tool and need to turn it into a digest for a sales
lead. Possibly implement helper functions or let the LLM summarize (with
careful prompting to avoid inaccuracies).

� 
Caching external info to avoid repeat calls in
one session.

o  
Content Generation and Verification: When
the agent drafts an email or response:

� 
Ensuring the tone and content match company
guidelines (could provide the agent a style guide in
the system prompt).

� 
Perhaps use a secondary validation step: e.g.,
run the draft through a moderation API or have a review-by-human step for
critical communications.

� 
Technical approach: you might have the agent
produce the draft, then your code flags certain keywords or uses another AI
model to score it for politeness/compliance before sending.

o  
Example Tie-in: Show a small C#
pseudo-code for a custom formatting tool that the CRM agent might use,
e.g., a tool that takes a draft and applies standard email signature or checks
spelling (though the model usually does fine, this is illustrative of custom
needs).

o  
Testing Conversational Agents: Note that
testing these agents might involve simulating conversation flows. Talk about
using transcripts or test cases to ensure it handles different customer
requests properly (perhaps a mention that this is
covered in lab or resources).

�       
Group 2 � Back-Office/Autonomous Agents
(Finance, Supply Chain, IT Ops):

o  
These agents often operate on data and
events, sometimes without direct user prompts (triggered by schedules or
alerts). Reliability and accuracy are paramount.

o  
Document & Data Processing: Many
internal agents must handle unstructured or large data:

� 
Finance example: parsing PDF invoices or
emails. Implementation: integrate an OCR library or use a pre-processing step.
Maybe the tool ParseInvoice(file) returns structured
data (and the agent then matches it with POs).

� 
IT Ops example: analyzing log files or
monitoring data. Implementation: possibly provide a tool to fetch recent logs,
and the agent uses its LLM ability to find anomalies. Could also use a regex or
ML-based log analyzer as a sub-tool.

� 
Discuss how to combine deterministic processing
with AI in these scenarios (e.g., first parse data to structure, then let agent
reason on it), to get the best of both.

o  
Scheduled or Event-Driven Execution:
Unlike interactive agents, some agents (IT ops, possibly supply chain) act when
an event occurs (an alert, a threshold crossed) rather than waiting for a user
message.

� 
Implementation: the agent could be invoked by a
background service. E.g., a scheduled job calls the Copilot SDK agent every
midnight to review inventory levels, or an event handler triggers the agent
when a server alert fires.

� 
How to maintain context in these cases: the
�conversation� might be one-off per event, but context could include recent
relevant data (like recent incidents, or last day�s inventory).

� 
Ensure idempotence if re-run: for example, if
the same alert triggers again, the agent should not duplicate an action
(address by logging actions or marking events as handled).

o  
Integration with Enterprise Systems:
These scenarios often tie into large systems (ERP, supply chain management,
ITSM tools). Implementation considerations:

� 
Using SDK tools to connect to such systems� APIs
or databases. E.g., a tool for �createJournalEntry�
that interfaces with an ERP�s API, or �openTicket(priority, details)� that creates an incident in a ticketing
system.

� 
Error handling if those systems are unavailable
or return errors (e.g., if the ERP API is down, the finance agent should catch
that and maybe email a human).

� 
Transaction management: e.g., if the agent does
two related actions (mark order shipped, deduct inventory), ensure either both
happen or it rolls back if one fails.

� 
Security: Use service accounts with limited
permissions for those API calls, and log what was done for compliance.

o  
Quality and Precision: In finance
especially, the tolerance for mistakes is low.

� 
Testing approach: before deploying, run the
agent on historical data to see if it would make correct matches (for
reconciliation) or any false flags.

� 
Possibly allow the agent to operate in a
�proposal mode� where it doesn�t directly write to production databases but
suggests actions for review (at least initially).

� 
Implementation note:
you can have a configuration flag that makes the agent�s actions read-only vs
write, toggling how it behaves depending on environment (dev/test vs prod).

o  
Human Override Mechanisms: How to
implement fallbacks where these agents defer to humans:

� 
e.g., If the finance agent finds an unusual
discrepancy, it could send an email or Teams message to the finance team with
details, rather than attempting a fix.

� 
In IT ops, if an agent is not confident or the
issue repeats after its first fix, it creates a ticket for a human.

� 
Technical: use callbacks or events from the
agent to trigger notifications. The agent might output something like �� that
your code interprets as a signal to involve a human.

� 
Monitoring dashboards: For these internal
agents, companies often build a small dashboard showing recent agent actions
and any pending approvals needed.

o  
Performance Considerations: If these
agents run on schedules or high frequency (like monitoring systems), be mindful
of rate limits and cost:

� 
Using the LLM frequently can have cost.
Strategies: adjust frequency (maybe batch tasks together), or
use a smaller model for less critical analyses and reserve big model for complex cases (the SDK�s multi-model routing can
help here)[21].

� 
For supply chain forecasting, maybe run heavy predictions once daily, not every minute.

o  
Example Tie-in: Possibly give an example
of an IT ops agent�s tool: RestartService(serverName) that runs a shell command. Show how you�d
implement that carefully (ensuring the serverName is
valid and the command is safe), illustrating the extra caution needed for such
powerful actions.

�       
Summary of Advanced Topics: Reiterate how
each scenario adds its �flavor�:

o  
Customer-facing = focus on conversation quality
and integration with user-facing systems.

o  
Back-office = focus on
data processing, event handling, and robust integration with enterprise
systems.

o  
However, none of these require changing the
fundamental architecture � they are extensions. The core (LLM + tools via
Copilot SDK) remains, we just plug in different tools or add logic around it.

�       
Conclusion/Prep for Lab: The unit wraps
up the conceptual and technical groundwork. Now that learners know what
to build and how to approach building it, the final unit will allow them
to get hands-on with one scenario (the e-commerce agent) to solidify these
concepts.

Unit 7: Hands-on Lab � Integrate an AI Agent into an E-commerce Web App
(Practical Exercise)

�       
Overview: In this final unit, learners
will apply what they�ve learned by adding an AI agent to a pre-built
e-commerce application. This is a guided lab (step-by-step exercise) using
the technologies specified:

a.     
Environment: Visual Studio Code for
editing, .NET 7/8 with C# for coding.

b.    
App Tech Stack: ASP.NET Core Web API
(server) + Blazor WebAssembly (client) � a
two-project solution representing the backend and front-end of the e-commerce
app.

c.     
Data: EF Core with a SQLite database for
simplicity (pre-seeded with sample product, order, and customer data).

d.    
AI SDK: GitHub Copilot SDK (C#/.NET
flavor) to incorporate the AI agent.

�       
Scenario Recap: The lab focuses on the E-commerce
Customer Service & Operations Agent scenario introduced earlier (Unit
3). The goal is to enable the web app to automatically handle a common customer
request � for example, checking order status and initiating a return/refund via
the AI agent.

�       
Pre-Lab Setup: Instructions for setting
up the project:

a.     
Ensure the learner has the code for the base
e-commerce app (it will be provided).

b.    
Steps to install the GitHub Copilot SDK NuGet
package for .NET (since it�s in preview, likely a specific GitHub source or
NuGet feed).

c.     
How to configure the Copilot SDK (e.g.,
obtaining an authentication token or client ID, if required).

�       
Lab Tasks: (The lab guide will walk
through these, but the outline enumerates them)

a.     
Define Agent�s Role & Tools: The
first coding task is to define what our agent can do:

� 
Implement in the Web API project a set of
functions (methods) that correspond to the agent�s abilities:

�       
Example tools: GetOrderDetails(orderId), ProcessRefund(orderId), SendCustomerEmail(orderId, message) etc. These will
use EF Core to query/update the SQLite database and perhaps integrate with a
fake email sender service.

�       
Add these tools to the Copilot SDK�s skill
registry with appropriate descriptions (so the LLM knows when to use them).

�       
Ensure to include safety checks in these
functions (e.g., don�t allow ProcessRefund if order
is already refunded or if beyond return period).

� 
In lab instructions, explicitly show how to
annotate the functions or config so that Copilot SDK exposes them to the agent.

b.    
Initialize the Copilot Agent: Next, set
up the agent in the app:

� 
Write code in Startup or in a service class to
initialize the CopilotClient, start a session (with a
chosen model, say GPT-4 or 3.5)[22].

� 
Provide a system prompt that gives the AI a
persona and instructions, e.g., �You are an AI assistant for ContosoShop. You can help customers with orders by checking
statuses, initiating returns, and answering questions. You have tools: GetOrderDetails, ProcessRefund, SendCustomerEmail. Use a friendly tone,� etc.

� 
The lab might instruct
how to keep this agent session alive or reuse it per
request.

c.     
Expose an API Endpoint for Chat/Request:
Create a new Web API controller (or use existing) that the Blazor client can
call when a user submits a question or request to the AI agent.

� 
This endpoint will take in the user�s query
(e.g., �I want to return my order 1234 because it arrived damaged�) and send it
to the Copilot SDK session.

� 
Show how to send the user prompt into session.sendMessage() or
equivalent and await the response from the agent.

� 
Handle streaming vs non-streaming response: to
keep it simple, maybe use non-streaming for now (get full answer, then return
to client).

� 
Parse the agent�s response and forward it to the
client. (If the SDK returns a structured result or final message, use that. The
agent might also produce an action that triggers our tools internally during
this call.)

d.    
Update Blazor Frontend: On the Blazor WebAssembly side, provide a simple UI for the lab:

� 
Perhaps a page �Contact Support� with a
chat-style interface or a form where user types a question.

� 
Connect this UI to the new Web API endpoint
(e.g., via HttpClient in Blazor). When user submits,
call the endpoint, then display the agent�s reply.

� 
If doing a chat UI, update it to append the
conversation (for simplicity, maybe the lab sticks to one-turn interactions or
explicitly states multi-turn will just be sequential calls with conversation
ID).

� 
Ensure the UI can show progress or a loading
indicator while waiting for the agent�s answer (since calling an AI model might
take a couple of seconds).

e.     
Test the Agent: Run the application
locally and simulate a few scenarios:

� 
For instance, the lab guide might say: �Try
asking the agent: �Where is my order 1001?� � it should fetch the status via GetOrderDetails and respond with tracking info. Next, ask
�I need to return my order 1001� � it should process the return via ProcessRefund and confirm.�

� 
Learners verify the agent�s behavior by checking
the database (seeing that order 1001�s status changed to �Refund Initiated�,
etc.) and the UI response.

� 
Also test edge cases as guided: e.g., ask for an
order that doesn�t exist (agent should handle gracefully), or a refund beyond
allowed period (agent should apologize and escalate).

f.      
Implement Logging (Bonus): If time
permits, the lab might include adding a simple logging mechanism:

� 
E.g., log each query and agent answer to a text
file or console, and maybe log each tool invocation inside the tool functions.
This reinforces the importance of audit logs discussed earlier.

�       
Lab Review and Takeaways: After
completing the exercise, the lab unit should recap what was learned:

a.     
How the common patterns (tools, planning) were
concretely implemented in the e-commerce scenario.

b.    
Challenges faced and how they were overcome
(like ensuring the agent didn�t refund twice, dealing with an unknown order
query, etc.).

c.     
Encourage thinking: How would you extend this
approach to the other scenarios? (e.g., what different tools or triggers would
you need for a finance agent or an IT ops agent?)

d.    
This cements knowledge and invites learners to
apply the pattern to their own use cases beyond this module.

By following this structured learning path, developers will
progress from understanding what AI agents are and why they�re useful,
through how to design and implement them with the GitHub Copilot SDK,
and finally to building a working agent within a realistic application.
The outline ensures that foundational knowledge is established before tackling
technical details, and that common concepts are mastered before dealing with
scenario-specific nuances. The final lab ties everything together, giving
hands-on experience with using the Copilot SDK in a modern .NET application to
solve a real-world problem. This comprehensive approach aims to equip
professional developers with both the conceptual framework and the practical
skills to integrate AI agents into enterprise systems confidently and
effectively.

Sources for reference:

�       
Mario Rodriguez, GitHub Blog: �Build
an agent into any app with the GitHub Copilot SDK� � introduced the SDK�s
capabilities and the complexity it abstracts[23]
[24].

�       
Kinfey, Microsoft
Dev Community: �GitHub Copilot SDK: core capabilities� � highlighted
SDK features (multi-turn loop, tool support, multi-language including .NET,
etc.)[25]
and the challenges of building agents that the SDK handles (context, tools,
safety, etc.)[26].

�       
AllCloud Blog: �Beyond
Chatbots: Building AI Agents�� � provided a clear definition of AI agents
vs chatbots and real-world examples of enterprise agents in various industries[27]
[28].

�       
(Scenario inspirations drawn from industry trend
reports and hypothetical use cases to illustrate practical applications.)

[1]https://allcloud.io/blog/beyond-chatbots-building-ai-agents/

[2]https://www.allganize.ai/en/blog/beyond-chatbots-why-enterprises-need-ai-agents-for-it-hr-and-customer-support

[3]https://www.allganize.ai/en/blog/beyond-chatbots-why-enterprises-need-ai-agents-for-it-hr-and-customer-support

[4]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[5]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[6]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[7]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[8]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[9]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[10]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[11]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[12]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[13]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[14]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[15]https://allcloud.io/blog/beyond-chatbots-building-ai-agents/

[16]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[17]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[18]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[19]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[20]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[21]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[22]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[23]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[24]https://github.blog/news-insights/company-news/build-an-agent-into-any-app-with-the-github-copilot-sdk/

[25]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[26]https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948

[27]https://allcloud.io/blog/beyond-chatbots-building-ai-agents/

[28]https://allcloud.io/blog/beyond-chatbots-building-ai-agents/


