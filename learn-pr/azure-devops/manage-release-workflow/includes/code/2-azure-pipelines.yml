- stage: 'Dev'
  displayName: 'Deploy to the Dev environment'
  dependsOn: Build
  jobs:
  - deployment: Deploy
    pool:
      vmImage: 'Ubuntu-16.04'
    environment: dev
    variables:
    - group: 'Release Pipeline'