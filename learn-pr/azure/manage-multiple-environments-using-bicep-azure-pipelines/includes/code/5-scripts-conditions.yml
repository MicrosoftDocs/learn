parameters: 
- name: environmentName
  type: string
  default: 'nonprod'
- name: serviceConnectionName
  type: string

steps:
- script: |
    echo Hello ${{parameters.environmentName}}!

- ${{ if eq(parameters.environmentName, 'production') }}:
  - script: |
      echo This step only runs for the production deployment.
