Update the template spec workflow file to lint the Bicep file

Update the template spec workflow file to publish the template spec with a very simple versioning scheme (just use the pipeline run number as the version number)

Commit, push, and run the workflow, then verify the template spec is created
