Refresh the page that showed the default NGINX page in your web browser. The load balancer distributes traffic to one of your VM instances, as shown in the following example:

![App from GitHub repo running on scale set]()

Leave the web browser open so that you can see an updated version in the next step.