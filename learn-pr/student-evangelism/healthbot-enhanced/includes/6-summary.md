Both interrupting and breaking scenarios break the current flow and start running a new scenario. The interrupting scenario returns to the main flow, whereas the breaking scenario doesn't. These interrupts are caused by a keyword, which we mention in the model of the scenario. 

To implement an interrupting scenario, we turn on **Interrupting scenario** and turn off **Breaking scenario**. To implement the breaking scenario, we turn on both options.
