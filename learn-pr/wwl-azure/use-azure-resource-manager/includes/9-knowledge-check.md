Choose the best response for each question. Then select Check your answers.

## Multiple Choice
Which one of these resource types can be moved across resource groups?
( ) Azure Backup Vault {{That's incorrect. An Azure Backup vault cannot be moved across resource groups.}}
( ) Azure Application Gateway {{That's incorrect. The Azure Application Gateway cannot be moved across resource groups.}}
(x) Azure Virtual Networks {{That's correct. Azure virtual network can be moved across resource groups.}}

## Multiple Choice
Which of the following would be good example of when to use a resource lock?
(x) A ExpressRoute circuit with connectivity back to your on-premises network. {{That's correct. This is a critical resources Resource locks prevent other users in your organization from accidentally deleting or modifying critical resources.}}
( ) A non-production virtual machine used to test occasional application builds. {{That's incorrect. This non-production resource would not need to be protected from accidental deletion or modification. }}
( ) A storage account used to temporarily store images processed in a development environment. {{That's incorrect. This temporary resource would not need to be protected from accidental deletion or modification.}}

## Multiple Choice
Which of the following features doesn't apply to resource groups?
( ) Resources can only be in one resource group {{That's incorrect. It does apply, as resources can be in only one resource group.}}
( ) Role-based access control can be applied to a resource group {{That's incorrect. It does apply, as role-based access control can be applied to the resource group.}}
(x) Resource groups can be nested. {{That's correct. It does not apply, as resource groups can't be nested.}}
 
