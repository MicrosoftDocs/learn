Choose the best response for each question. Then select Check your answers.

## Multiple Choice
Azure Monitor organizes log data into which of the following?
(x) Tables {{That's correct. Azure Monitor log data is organized in tables.}}
( ) Event queues {{That's incorrect. Azure Monitor log data isn't organized in event queues. Is there another more structured option?}}
( ) Text files {{That's incorrect. Text files would not be an efficient way to organize log data. }}

## Multiple Choice
An online retailer has a very large web farm with more than 100 virtual machines. They would like to use Log Analytics to ensure these machines are responding to requests. To automate the process, the team will create a search query. Which source table should be used?
( ) Event {{That's incorrect. The Events table logs Windows computer events.}}
( ) SysLog {{That's incorrect. The Syslog table logs Linux computer events.}}
(x) Heartbeat {{That's correct. The Heartbeat table reports on agent health.}}

## Multiple Choice
Log analytics agents can run on which of the following?
( ) Only on physical computers. {{That's incorrect. Log analytic agents can also run on virtual computers.}}
( ) Only on cloud computers. {{That's incorrect. Log analytic agents can also run on on-premises computers. }}
(x) on many different platforms including other providers. {{That's correct. Log analytics agents can run on many different platforms including other providers.}}
 
