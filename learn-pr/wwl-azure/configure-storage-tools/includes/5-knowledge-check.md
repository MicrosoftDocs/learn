Choose the best response for each question. Then select Check your answers.

## Multiple Choice
A manufacturing company's finance department wants to control how the data is being transferred to Azure Files. They want a graphical tool to manage the process, but they don't want to use the Azure portal. What tool would be best in this situation?
( ) Azure Data Box {{That's incorrect. Data Box would be better for importing and exporting very large data sets to Azure. }}
( ) Robocopy {{That's incorrect. Robocopy would be better for copying large datasets or lots of files across volumes.}}
(x) Azure Storage Explorer {{That's correct. Azure Storage Explorer is the best choice of the finance department doesn't want to use the Azure portal.}}

## Multiple Choice
These is an existing storage account in Azure that stores unstructured data. The organization creates a new storage account. What is the best way to move the existing data to the new storage account?
( ) Use the Azure portal {{That's incorrect. Is there another Azure tool more focused on storage?}}
( ) Use the Robocopy command-line tool {{That's incorrect. It will be difficult to move data between cloud storage accounts. }}
(x) Use the AzCopy command-line tool {{That's correct. The key in this scenario is to move data between storage accounts. The AzCopy tool can work with two different storage accounts. The other tools do not copy data between storage accounts. Alternatively, although not one of the answer choices, Storage Explorer can be used to copy data between storage accounts.}}

## Multiple Choice
The finance team needs to transfer a series of large files to blob storage. It may take several hours to upload each file. Everyone is concerned if the transfer fails, it will have to be restarted. Which tool is the most appropriate for this task?
( ) Azure CLI  {{That's incorrect. Is there another tool that might more appropriate for this task?}}
(x) AzCopy {{That's correct. AzCopy is ideal for transferring large files as it can run in the background.}}
( ) Azure Storage Explorer {{That's incorrect. Is there another tool that might more appropriate for this task?}}

