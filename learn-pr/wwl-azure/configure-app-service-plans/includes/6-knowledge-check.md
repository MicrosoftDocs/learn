Choose the best response for each question. Then select Check your answers.

## Multiple Choice
The infrastructure team is responsible for managing a production web app. The app requires scaling to five instances, 40GB of storage, and a custom domain name. A least cost solution is desired. Which App Service Plan would meet the requirements?
( ) Basic {{That's incorrect. The Basic App Service plan doesn't not meet the requirements. }}
(x) Standard {{That's correct. The Standard App Service plan meets the requirements at the least cost. }}
( ) Premium {{That's incorrect. The Premium App Service plan meets the requirements but is not the most cost effective. }}

## Multiple Choice
Which of following provides more CPU, memory, or disk space without adding more virtual machines?
(x) Scale up {{That's correct. Scale up gives more CPU, memory, and disk space.. Scale up by changing the pricing tier of the App Service plan.}}
( ) Scale out {{That's incorrect. Scale out increases the number of virtual machine instances that run the app.}} 
( ) Scale back {{That's incorrect. Scale back is not a valid choice.}}

## Multiple Choice
Triggering a webhook at 8am on Saturday is an example of which of the following? 
( ) A metric-based rule. {{That's incorrect. Metric-based rules measure application load and add or remove virtual machines based on that load.}}
( ) An app-insight rule. {{That's incorrect. App-insight rule is not a valid choice. }}
(x) A time-based rule. {{That's correct. Time-based rules allow scaling based on time patterns.}}
 
