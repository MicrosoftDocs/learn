Choose the best response for each question. Then select Check your answers.

## Multiple Choice
What is the Azure ExpressRoute service?
​( ) Azure ExpressRoute is a service that provides a VPN connection between on-premises and the Microsoft cloud. {{That's incorrect. Azure ExpressRoute does not use a VPN connection.}}
(x) Azure ExpressRoute is a service that provides a direct connection from your on-premises datacenter to the Microsoft cloud. {{That's correct.}}
( ) Azure ExpressRoute is a service that provides a site-to-site VPN connection between your on-premises network and the Microsoft cloud. {{That's incorrect. Azure ExpressRoute does not use a site-to-site VPN connection.}}

## Multiple Choice
What Microsoft service helps to simplify a complex hub-and-spoke virtual network WAN deployment?
​(x) Azure Virtual WAN {{That's correct. Azure Virtual WAN with Virtual WAN hubs simplifies a complex virtual network WAN. }}
( ) ExpressRoute {{That's incorrect. Another choice focuses on complex WAN deployments.}}
( ) Virtual network peering {{That incorrect. Virtual network peering is best for simple deployments.}}

## Multiple Choice
When should Azure ExpressRoute be used instead of Azure site-to-site connectivity?
( ) For connecting mobile users directly to your virtual network running in Azure. {{That's incorrect. Use a point-to-site connection for this scenario.}}
( ) For handling small-scale production workloads running on Azure virtual machines. {{That's incorrect. Consider using a site-to-site connection for this scenario.}}
(x) For handling enterprise-class and mission-critical workloads. {{That's correct.}}
