Use the following questions to check what you’ve learned in this module.

### Multiple Choice

1.  Which of these is the common structure that PowerShell cmdlets use?

(x)Verb-Noun\{\{Correct. The common structure that PowerShell cmdlets use is the Verb-Noun format. The verb portion of the cmdlet name indicates what the cmdlet does. The noun portion of the cmdlet name indicates what kinds of resources or objects the cmdlet affects.\}\}

( )Noun-Verb\{\{Incorrect. The common structure that PowerShell cmdlets use is the Verb-Noun format. The verb portion of the cmdlet name indicates what the cmdlet does. The noun portion of the cmdlet name indicates what kinds of resources or objects the cmdlet affects.\}\}

( )There is no specific structure that PowerShell cmdlets use.\{\{Incorrect. The common structure that PowerShell cmdlets use is the Verb-Noun format. The verb portion of the cmdlet name indicates what the cmdlet does. The noun portion of the cmdlet name indicates what kinds of resources or objects the cmdlet affects.\}\}

---

2.  Which key can you use to complete or suggest parameters for a PowerShell command?

( )Enter key\{\{Incorrect. You can use the Tab key to invoke Tab completion when entering a PowerShell command. This feature improves the speed and ease of finding and entering cmdlets and parameters.\}\}

(x)Tab key\{\{Correct. You can use the Tab key to invoke Tab completion when entering a PowerShell command. This feature improves the speed and ease of finding and entering cmdlets and parameters.\}\}

( )End key\{\{Incorrect. You can use the Tab key to invoke Tab completion when entering a PowerShell command. This feature improves the speed and ease of finding and entering cmdlets and parameters.\}\}
