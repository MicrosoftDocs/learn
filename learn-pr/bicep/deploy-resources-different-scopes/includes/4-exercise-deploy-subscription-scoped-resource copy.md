1. Create a new Bicep file and set its `targetScope` to `subscription()`.
2. Add an Azure Policy definition to limit the VM SKUs.
3. Assign the policy to the subscription.
4. Deploy the Bicep template.